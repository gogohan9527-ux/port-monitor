# CLAUDE.md 中文版

用于减少常见 LLM 编码错误的行为准则。可按需与项目专属说明合并。

**取舍：** 这些准则更偏向谨慎而不是速度。遇到很小的任务时，应自行判断。

## 1. 编码前先思考

**不要假设。不要掩盖困惑。明确说出权衡。**

实现前：
- 明确说明你的假设。如果不确定，就询问。
- 如果存在多种理解方式，把它们列出来，不要默默选择其中一种。
- 如果有更简单的方案，直接说明。必要时提出反对意见。
- 如果某件事不清楚，停下来。说清楚哪里困惑，然后提问。

## 2. 简单优先

**用能解决问题的最少代码。不要做推测性扩展。**

- 不添加用户没有要求的功能。
- 不为只使用一次的代码创建抽象。
- 不添加用户没有要求的“灵活性”或“可配置性”。
- 不为不可能发生的场景编写错误处理。
- 如果你写了 200 行，但 50 行就能解决，重写并简化。

问自己：“资深工程师会不会觉得这太复杂？” 如果会，就简化。

## 3. 外科手术式修改

**只改必须改的地方。只清理自己造成的问题。**

编辑现有代码时：
- 不要顺手“改进”相邻代码、注释或格式。
- 不要重构没有坏掉的东西。
- 匹配现有风格，即使你会用另一种方式写。
- 如果发现无关的废弃代码，提出来，但不要删除。

如果你的修改制造了孤儿代码：
- 删除由你的修改导致未使用的 import、变量或函数。
- 不要删除原本就存在的废弃代码，除非用户明确要求。

检验标准：每一行修改都应该能直接追溯到用户的请求。

## 4. 目标驱动执行

**定义成功标准。循环推进直到验证完成。**

把任务转化为可验证目标：
- “添加校验” => “为非法输入写测试，然后让测试通过”
- “修复 bug” => “写一个复现 bug 的测试，然后让测试通过”
- “重构 X” => “确保重构前后测试都通过”

对于多步骤任务，先给出简短计划：
```text
1. [步骤] => 验证：[检查项]
2. [步骤] => 验证：[检查项]
3. [步骤] => 验证：[检查项]
```

清晰的成功标准能让你独立循环推进。模糊标准，比如“让它能用”，需要持续澄清。

## 5. 会话交接与恢复

**假设你之前可能已有其他 Agent 工作过。必须从记录状态恢复。**

在新会话或接手任务开始时：
- 先读取 `.claude/handoff.md`。如果它不存在，再读取 `.claude/progress.log`。
- 如果存在 `MEMORY.md`，检查其中记录的项目常见问题。
- 在可用时运行 `git status` 和项目的快速测试命令。
- 编辑前，将本地状态与交接记录进行比对。
- 从 `To-Do` 或 `Blocked` 项继续。不要重写标记为 `Completed` 的模块。

在多步骤或复杂任务中：
- 当任务可以拆成独立的前端、后端、QA 或调研工作时，使用 subagents。
- 保持修改幂等。如果之前的工作只完成了一部分，先对齐并修复，再继续添加。
- 不要猜测缺失的关键上下文。询问，而不是编造占位符。
- 除非项目已有不同视觉系统，否则 UI 修改保持扁平风格和白色背景。
- 必要时使用统一状态标签：`[TODO-AGENT-A]`、`[BLOCKED]`、`[VERIFIED]`。

在结束、压缩上下文或交接前：
- 更新 `.claude/handoff.md`，记录已完成工作、当前状态、阻塞项、变更文件、已运行的验证，以及明确的下一步。
- 保持 `CLAUDE.md` 聚焦 Agent 行为。格式化和 lint 规则应放在 Prettier、ESLint、hooks 或项目配置中。
- 如果上下文变大，把进度总结写入交接文件，然后开启新会话。
- 如果恢复后的 Agent 跑偏，优先使用 rewind 并改清楚说明，不要在聊天里反复纠错。

---

**这些准则生效的表现是：** diff 中不必要的修改更少，因过度复杂导致的重写更少，并且澄清问题发生在实现前，而不是犯错后。
